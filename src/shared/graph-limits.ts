/** Shared bound for graph controls, admissions, persistence and export. */
export const MAX_VISIBLE_NODES = 15_000;

/** Keep camera division finite without imposing a visible zoom-out limit. */
export const MIN_GRAPH_ZOOM = Number.EPSILON;
